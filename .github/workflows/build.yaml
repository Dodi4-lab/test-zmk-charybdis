name: Build ZMK Firmware

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:   # позволяет запустить вручную кнопкой на Actions

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main